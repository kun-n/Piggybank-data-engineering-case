CREATE TABLE client (
    CUSTOMER_ID    INTEGER NOT NULL,
    REPORTING_DATE TEXT    NOT NULL,
    EDUCATION      TEXT    NOT NULL,
    AGE            INTEGER NOT NULL,
    BUCKET         INTEGER NOT NULL,
    PRIMARY KEY (CUSTOMER_ID, BUCKET)
);